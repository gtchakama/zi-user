{
  "name": "zi-user",
  "version": "1.0.1",
  "description": "Zi-User is a simple package that generates random user objects with fake names and passwords. It uses an object containing arrays of fake first names and last names to generate random name combinations. It also generates a random email address and password.",
  "main": "index.js",
  "scripts": {
    "test": "node test.js"
  },
  "author": "gtchakama",
  "license": "ISC",
  "dependencies": {
    "zi-user": "^1.0.1"
  }
}
